# خطة تنفيذ المرحلة الثانية — مشروع تواسع

> وثيقة تشغيلية مستمدة من `plan.md` (النسخة المعتمدة) — المرحلة **الثانية** من مراحل التنفيذ (§12 بند 2 في `plan.md`).
> **النسخة 2 (مراجعة صاحبة المشروع):** بناء المحرك على **طبقات مستقلة متدرجة**، مع **بيئة تجربة مؤقتة في المتصفح**، و**اختبارات معزولة** تُضمَّن عند نجاحها فقط.
> **لا يُنفَّذ أي كود قبل موافقة صاحبة المشروع الصريحة على هذه الوثيقة.**

---

## 1) الهدف والنطاق

- **الهدف:** تنفيذ **المحرك الحسابي** بمنهج **طبقات متدرجة** (الحفظ الجديد ← المراجعة الصغرى ← المراجعة الكبرى ← الدمج)، بحيث يظهر الخلل في طبقته دون كسر أي شيء آخر، مع **بيئة تجربة مؤقتة** تُراجَع يدوياً في المتصفح بعد كل طبقة، و**اختبارات معزولة** تُضمَّن في المشروع عند نجاحها فقط، إضافة إلى **تجربة PDF عربية مبكرة** لكشف المخاطرة.
- **النطاق يشمل:** محرك المصحف (قراءة الفهرس المدمج)، طبقات المحرك الثلاث وطبقة الدمج، التحقق المشترك من مدخلات الخطة، بيئة التجربة المؤقتة، الاختبارات المعزولة، وأداة تجربة PDF العربية.
- **خارج النطاق (مراحل لاحقة):** طبقة التخزين (IndexedDB)، الشاشات الحقيقية، المعاينة القابلة للتعديل والتراجع، توليد الـ PDF كاملاً وتنزيله، الإعدادات. كل ما يُبنى هنا من أدوات تجريبية (بيئة المتصفح، الاختبارات المعزولة) **مؤقت ويُحذف قبل الإنتاج**.

---

## 2) القرارات الملزمة المنقولة من `plan.md`

| القرار | المرجع في `plan.md` |
|---|---|
| المحرك منطق خالص: لا يعرف الشاشة ولا التخزين، قابل للنقل والاختبار | §6-2 |
| كل مرجع "سورة:آية" يُرسم من الفهرس المدمج، لا نصاً مكتوباً يدوياً | §3 |
| نصف الوجه يُحسب من الأسطر الفعلية لكل صفحة (من الفهرس) — لا افتراض ثابت لعدد الأسطر | §8 |
| المؤشر يتحرك آيةً آية حسب الاتجاه | §8-3 |
| سرد التثبيت/الرفيق: لا حفظ جديد | §8-5، §8-6 |
| أول يوم دراسي بعد السرد الأسبوعي: لا صغرى فيه إطلاقاً | §8-4 |
| المراجعة الكبرى: **كل يوم دراسي** بمقدار تحدده المعلمة (من نصف وجه إلى جزء كامل)، **بلا سقف جزاء**، وتعود الدورة من البداية | §8-7 |
| اكتمال سورة: تخرج من الصغرى وتدخل ضمن الكبرى، وتُفتح سورة جديدة | §8-8 |
| المعكوس يتوقف عند نهاية الفاتحة دون آيات وهمية | §8 |
| المفتوح يُحسب له يوم نهاية ويُعرض صراحةً | §8 |
| مدخلات المستخدم تُتحقق في طبقة مشتركة واحدة | §2 |
| اختبارات المحرك حسب قائمة `plan.md` §11 | §11 |

---

## 3) قراءات من المصحف (طبقة المحرك)

- وحدة واحدة `src/engine/mushaf.ts` هي **الواجهة الوحيدة** التي تقرأ الفهرس المدمج `src/data/mushaf-index.json` (مع تعليق يذكر ملف المصدر المعتمد، حسب قاعدة سلامة البيانات القرآنية).
- تُستورد بيانات الفهرس مباشرةً كوحدة — لا قراءة ملفات، لا شبكة.
- الواجهة تُصدّر دوالاً خالصة (Pure functions) لا حالة لها:
  - `refExists(ref)`، `isRefInRange(ref, first, last)`.
  - `nextRef(ref)` / `prevRef(ref)` (باحترام حدود القرآن).
  - `surahStart(surah)` (أول آية من السورة من الفهرس، لا من ذاكرة).
  - `pageOf(ref)`، `lineIndexOf(ref)`.
  - `endOfFace(ref)`: نهاية الوجه الحالي — أسطر الصفحة الآية تُقسَّم إلى وجهين متتاليين: الوجه الأول = النصف الأول من أسطر الآيات (مقرباً للأعلى)، والثاني = الباقي. يُرجع آخر آية في وجه المرجع.
  - `endOfPage(ref)`: آخر آية في الصفحة الحالية.
  - `endOfQuran` (= 114:6) و `endOfFatihah` (= 1:7) — مرسومة من الفهرس.
  - `countAyahs(first, last)`: عدد الآيات في النطاق (بالترتيب).
  - **بيانات الأجزاء (قرار أ-أ):** `juzOf(ref)`، `juzEndOf(ref)`، `juzAyahCount(juz)` — من بيانات الأجزاء في الفهرس (المصدر: QUL مورد 68 «Juz») — بيانات مرجعية تبقى في الفهرس، **بلا استخدامها سقفاً للمراجعة الكبرى** (أُلغي السقف بقرار المستخدمة، انظر §6).
- **كل القيم مشتقة من الفهرس**؛ لا يُدخل أي رقم "سورة:آية" في الشفرة من خارج الفهرس (ممنوع).

### قرار أ (معتمد — أ-أ): حدود الأجزاء من QUL
- مورد QUL (68) «Juz — Juz info» بصيغة JSON: لكل جزء (1–30) `first_verse_key` و `last_verse_key` و `verses_count` — **بيانات مرجعية فقط، بلا نصوص**.
- تُضاف مادة موثقة في `data/qul-source-manifest.json`، وتُضاف بيانات `juzes` للفهرس عبر أداة البناء (إعادة بناء آمنة بأداتنا).
- **أُلغي سقف المراجعة الكبرى بالجزء (قرار المستخدمة):** المقدار اليومي تحدده المعلمة بلا سقف ثابت؛ بيانات الأجزاء تبقى في الفهرس كبيانات مرجعية فقط (حدود الأجزاء للعرض/التوثيق).
- سُجِّل القرار في `plan.md` (سجل التعديلات، بند 5) وفي `data/qul-source-manifest.json`.

---

## 4) مدخلات الخطة ونموذجها

### 4.1 `PlanConfig` (تُنشأ من الشاشات لاحقاً، يتحقق منها `shared/validation`)
- `kind`: `closed` (بداية ونهاية تاريخان) | `open` (عدد أسابيع).
- `direction`: `forward` | `reverse`.
- `startRef`: `{surah, ayah}` — نقطة البداية (يُتحقق من وجودها في الفهرس).
- `rate`: `{ type: 'half-page' } | { type: 'full-page' } | { type: 'ayahs', count: number }`.
- `weeklyPattern`: مصفوفة من 7 أنماط أيام (دراسة / سرد تثبيت / سرد مع رفيق / إجازة).
- `dates` (للمحدد): `{ start: 'YYYY-MM-DD', end: 'YYYY-MM-DD' }`.
- `weeks` (للمفتوح): عدد صحيح موجب.
- `grandReview`: `{ ayahsPerRound: number }` — تُطبَّق **كل يوم دراسي** بمقدار يومي تحدده المعلمة (من نصف وجه إلى جزء كامل، **بلا سقف**).

### 4.2 `DayPlan` (مخرجات لكل يوم)
- `date` (الميلادي)، `kind` (دراسة | سرد تثبيت | سرد مع رفيق | إجازة).
- `newMemorization`: نطاق `{from, to}` أو `null`.
- `smallReview`: نطاق أو `null`.
- `grandReview`: نطاق أو `null`.
- `phrase`: نص العبارة لأيام الإجازة (قرار د) أو `null`.

### 4.3 `ComputeResult`
- `days: DayPlan[]`.
- `endRef`: آخر مرجع وصل إليه الحفظ الجديد.
- `completedSurahs: number[]`.
- `issues: string[]` — تنبيهات (لا تُوقف الحساب).

---

## 5) بناء المحرك على طبقات مستقلة متدرجة

> **الفكرة:** المحرك يُبنى في **4 طبقات**، تُنجز واحدة تلو الأخرى. لا تبدأ طبقة جديدة قبل نجاح سابقتها **بشهادة مزدوجة**: (1) اختباراتها المعزولة كلها تمر، (2) **فحصك اليدوي** في بيئة المتصفح المؤقتة وموافقتك. عند نجاح الطبقات الثلاث الأولى تُبنى طبقة الدمج، ثم يُختبر المحرك كاملاً. أي خلل يظهر يبقى **محصوراً في طبقته** دون المساس بما سبقها.

### 5.1 بوابات التقدم (Gates)

| البوابة | الشرط | تعني |
|---|---|---|
| G1 | طبقة الحفظ الجديد | اختباراتها المعزولة تمر + فحصك في المتصفح + موافقتك |
| G2 | طبقة المراجعة الصغرى | الاختبارات تمر + فحصك + موافقتك |
| G3 | طبقة المراجعة الكبرى | الاختبارات تمر + فحصك + موافقتك |
| G4 | طبقة الدمج | اختبارات المحرك الكاملة تمر + فحصك النهائي |

- خلال كل ذلك: `npm run verify` (البوابة الكاملة للمرحلة الأولى) **يبقى أخضر دائماً** — لا كسر أبداً.
- كل طبقة تُبنى في ملف/ملفات مستقل: `src/engine/memorization.ts`، `src/engine/small-review.ts`، `src/engine/grand-review.ts`، `src/engine/merge.ts` — بلا تداخل.

### 5.2 طبقة 1 — الحفظ الجديد (`memorization.ts`)
- **الدخل:** `PlanConfig` (بعد التحقق المشترك) + دوال المصحف (`mushaf.ts`).
- **الخرج:** `MemorizationSchedule`:
  - هيكل أيام الخطة: لكل يوم `date` ونوعه (من النمط الأسبوعي والمدة).
  - لكل يوم: نطاق الحفظ الجديد `{from, to}` أو `null`.
  - سجل «اكتمال السور»: متى اكتملت كل سورة (يستهلكه السرد/الصغرى/الكبرى لاحقاً).
  - `endRef` + مؤشر تقدم الحفظ بعد كل يوم (يتحرك آيةً آية).
- **القواعد:**
  - المؤشر يبدأ من `startRef` ويسير آيةً آية بالاتجاه.
  - أيام الدراسة فقط تحمل حفظاً جديداً؛ السرد تثبيت/الرفيق والإجازة: لا حفظ.
  - المعدلات الثلاثة:
    - `نصف وجه`: من المؤشر إلى `endOfFace(marker)` — البدء من منتصف صفحة يكمل الوجه الحالي حتى حدّه (قرار ب).
    - `وجه كامل`: من المؤشر إلى `endOfPage(marker)`.
    - `عدد آيات`: `count` آيات.
  - عند اكتمال آخر آية في سورة: تُسجَّل السورة مكتملة، ويبدأ الحفظ من السورة الجديدة.
  - حدود التوقف: أمامي عند `endOfQuran`، معكوس عند `endOfFatihah` — **دون آيات وهمية**؛ إن حدث قبل انتهاء المدة يتوقف الحفظ ويُحتسب يوم النهاية الفعلي.
  - للمفتوح: استهلاك الأسابيع كلها (أو التوقف المبكر أعلاه).

### 5.3 طبقة 2 — المراجعة الصغرى (`small-review.ts`)
- **الدخل:** `PlanConfig` + `MemorizationSchedule` (من طبقة 1) + دوال المصحف.
- **الخرج:** لكل يوم نطاق الصغرى `{from, to}` أو `null`.
- **القواعد (آلية المعلمة المعتمدة):**
  - يوم **سرد تثبيت / سرد رفيق**: الصغرى = النطاق الكامل للسورة الجارية (أمامي: `surahStart` → المؤشر؛ معكوس: المؤشر → `surahEnd`).
  - **يوم دراسي عادي**: الصغرى = **نطاق الحفظ الجديد ليوم الدراسة السابق** كاملاً كما هو — حتى لو عبر حد سورة (أُتمّت سورة وبدأت أخرى في يوم واحد: تُشمل كاملة).
  - **أول يوم دراسي بعد يوم السرد الأسبوعي (تثبيت أو رفيق)**: لا صغرى فيه إطلاقاً (`null`).
  - نمط **بلا أيام سرد**: كل يوم دراسة = حفظ اليوم السابق؛ وأول يوم دراسة في الخطة بلا صغرى (لا يوجد حفظ سابق).
  - أيام الإجازة: `null`.

### 5.4 طبقة 3 — المراجعة الكبرى (`grand-review.ts`)
- **الدخل:** `PlanConfig` + `MemorizationSchedule` (من طبقة 1).
- **الخرج:** لكل يوم نطاق الكبرى `{from, to}` أو `null`.
- **القواعد (آلية المعلمة المعتمدة):**
  - بركة المحفوظ المكتمل = السور المكتملة **عدا السورة الجارية** (من سجل طبقة 1)، كنطاق متصل (أمامي: من أول سورة مكتملة إلى آخرها؛ معكوس: من آخرها إلى أولها).
  - تُطبَّق **كل يوم دراسي** (بلا دورية)؛ اليوم يأخذ `ayahsPerRound` من البركة — مقدار يومي تحدده المعلمة (من نصف وجه إلى جزء كامل، **بلا سقف جزاء**).
  - عند بلوغ نهاية البركة تعود الدورة من بدايتها حتى تنضم سور جديدة.
  - بركة فارغة (لا سور مكتملة بعد): الكبرى = `null`.

### 5.5 طبقة 4 — الدمج والمخرجات النهائية (`merge.ts`)
- **الدخل:** `PlanConfig` + مخرجات الطبقات الثلاث.
- **الخرج:** `ComputeResult` كاملة.
- **القواعد:**
  - دمج أعمدة الأيام (الحفظ الجديد / الصغرى / الكبرى) في `DayPlan[]`.
  - التواريخ الميلادية محسوبة من البداية عبر `Date` بإضافة أيام فقط (بلا مناطق زمنية).
  - `phrase` لأيام الإجازة = النص الثابت (قرار د) أو `null`.
  - `endRef` و`completedSurahs` و`issues` تُجمع من الطبقات.

### 5.6 التبعيات (عقود بين الطبقات)
- طبقة 1 مستقلة (لا تعتمد على 2 أو 3). طبقة 2 تعتمد على مخرجات 1 فقط. طبقة 3 تعتمد على مخرجات 1 فقط. طبقة 4 تجمع الكل.
- قاعدة صارمة: **لا تقرأ أي طبقة مخرجات طبقة لم تُبنَّ بعد، ولا تعدّل بيانات طبقة أخرى** — كل طبقة تعمل على دخلها وتُخرج نتاجها.

---

## 6) القرارات المعتمدة في هذه المرحلة

| # | القرار | الحالة |
|---|---|---|
| أ | حدود الأجزاء: من QUL (فهرس) | **بيانات مرجعية في الفهرس** (قرار أ-أ) — بقي `juzOf`/`juzEndOf`/`juzAyahCount` كدوال بيانات؛ **أُلغي سقف «جزء واحد يومياً»** بقرار المستخدمة: المقدار اليومي تحدده المعلمة بلا سقف ثابت |
| ب | نصف/وجه كامل عند البدء من منتصف صفحة | **مُقرر:** يكمل الوجه/الصفحة الحالية حتى حدّها (لا قفز) |
| ج | سقف أقصى لمدة الخطة (محدد) | **مُقرر:** **730 يوماً** |
| د | عبارة يوم الإجازة | **مُقرر:** نص ثابت افتراضي = «قال النبي ﷺ: "تَعَاهَدُوا هذا القُرْآنَ، فَوَالذي نَفْسُ مُحَمَّدٍ بِيَدِهِ لَهُوَ أشَدُّ تَفَلُّتاً مِنَ الإِبِلِ في عُقُلِهَا"» — قابل للتعديل يدوياً لاحقاً؛ ومكتبة العبارات في نسخة ثانية |

---

## 7) بيئة التجربة المؤقتة (Sandbox) — للفحص اليدوي في المتصفح

- **الغرض:** بعد بناء كل طبقة، تفحصين مخرجاتها بنفسك في المتصفح (بلا أي واجهة حقيقية للتطبيق).
- **الموقع:** مجلد `dev/sandbox/` — **معزول تماماً عن التطبيق**:
  - يُخدم في وضع التطوير فقط على مسار خاص (مثل `/sandbox/` عبر `npm run dev`).
  - **مستثنى من البناء الإنتاجي** (vite يبني `index.html` الرئيسي فقط — الساندبوكس لا يصل للإنتاج أبداً).
- **الشكل (بسيط):** نماذج/حقول لإدخال مدخلات الخطة (أو مدخلات الطبقة الجارية)، وجدول مبسط يعرض مخرجاتها (أيام/نطاقات/أجزاء).
- **دورة الاستخدام:** بعد كل طبقة تُحدَّث البيئة لتعرض مخرجات تلك الطبقة؛ عند طبقة الدمج تعرض المحرك كاملاً.
- **الحذف:** تُحذف بيئة التجربة نهائياً عند بناء شاشات التطبيق الحقيقية (المرحلة 4) — لا يبقى أثر منها في الإنتاج.

---

## 8) استراتيجية الاختبار — اختبارات معزولة تُضمَّن عند النجاح

### 8.1 مبدأ العزل
- الاختبارات قيد التطوير تُكتب في منطقة **معزولة**: `scratch/tests/` — **مستثناة** من إعدادات المشروع (vitest config الرئيسية وtsconfig)، و**مستثناة من git** (تُضاف إلى `.gitignore`).
- `npm test` و`npm run verify` **لا يريان منطقة scratch إطلاقاً** — لا كسر أبداً أثناء التعديل.

### 8.2 دورة العمل (اكتب ← جرّب ← أصلح ← اعتمد)
1. أكتب اختبارات الطبقة الجارية في `scratch/tests/`.
2. أشغّلها بأمر **معزول** مستقل (مثل `npm run test:scratch` بمهمة vitest بإعداد منفصل) — لا يتدخل مع `npm test`.
3. إن **فشل** اختبار: أبقيه في scratch وأصلح المحرك/الاختبار وأعيد حتى ينجح — دون أي أثر على المشروع.
4. إن **نجح** اختبار واعتمدناه: يُنقل إلى مجلد الاختبارات الرسمي `tests/engine/` ويُضمَّن تلقائياً في `npm test` و`npm run verify`.
5. عند اكتمال الطبقة (كل اختباراتها المعتمدة في `tests/engine/`): تُغلق بوابة G.

### 8.3 قائمة الاختبارات (الهدف النهائي — من `plan.md` §11)
- الحفظ: المحدد والمفتوح، الأمامي والمعكوس، المعدلات الثلاثة (من الأسطر الفعلية للفهرس).
- نمط أسبوع بلا أيام سرد.
- الصغرى: يوم دراسي = حفظ اليوم السابق كاملاً (حتى لو عبر سورة)، أول يوم بعد السرد بلا صغرى، صغرى السرد = النطاق الكامل للسورة الجارية.
- الكبرى: كل يوم دراسي، المقدار اليومي (بلا سقف جزاء)، العودة من البداية، البركة الفارغة.
- انتقال السورة من الصغرى إلى الكبرى وبدء سورة جديدة.
- نهاية الخطة في منتصف سورة؛ المعكوس يتوقف عند نهاية الفاتحة بلا آيات وهمية.
- العبارات تظهر في أيام الإجازة فقط.
- الحالات المرفوضة (§9) برسائل واضحة.
- كل "سورة:آية" في أي مخرج مُرسم من الفهرس (فحص آلي).
- `npm run verify` يبقى أخضر (لا كسر للمرحلة الأولى).

---

## 9) الحالات الفارغة والرفض (fail-closed)

| الحالة | السلوك |
|---|---|
| نمط أسبوع بلا أيام دراسة | رفض برسالة واضحة |
| تواريخ معكوسة أو مدة ≤ 0 | رفض |
| معدل صفري (عدد آيات = 0) | رفض |
| مدة تتجاوز السقف الأقصى (730 يوماً) | رفض |
| نقطة بداية خارج القرآن / قبل الفاتحة في المعكوس | رفض |
| نطاق آية غير موجود في الفهرس (لا يُتوقع حدوثه) | إيقاف الحساب برسالة (لا تصحيح صامت) |
| بركة الكبرى فارغة | كبرى = `null` (سلوك طبيعي لا رفض) |

---

## 10) تجربة PDF عربية مبكرة (كشف المخاطرة أولاً — `plan.md` §12-2)

- **الهدف:** إثبات أن pdfmake + خط عربي مضمّن ينتجان ملف PDF بجدول عربي RTL سليم، قبل الالتزام بالمرحلة الخامسة.
- **الخطوات:**
  1. إضافة pdfmake، وتحميل خط **IBM Plex Sans Arabic** (رخصة OFL) مرة واحدة إلى `src/pdf/fonts/` (خطوة شبكة واحدة).
  2. أداة dev-only `tools/pdf-trial.ts` تولّد عيّنة PDF (عنوان عربي + جدول + RTL) وتكتبها ملفاً للفحص البصري.
  3. **فحص بصري بشري:** ظهور النص العربي سليماً، ترتيب الأعمدة RTL، الخط مضمّناً.
  4. النجاح ⇒ يُؤجَّل الدمج الكامل داخل التطبيق للمرحلة الخامسة. الفشل ⇒ وقف واستشارة.
- الأداة تبقى dev-only ولا تُضمَّن في التطبيق.

---

## 11) معايير القبول (قابلة للقياس)

- [ ] **G1:** طبقة الحفظ الجديد — اختباراتها المعزولة كلها تمر، وفحصك اليدوي في المتصفح، وموافقتك.
- [ ] **G2:** طبقة المراجعة الصغرى — اختباراتها تمر + فحصك + موافقتك.
- [ ] **G3:** طبقة المراجعة الكبرى — اختباراتها تمر + فحصك + موافقتك.
- [ ] **G4:** طبقة الدمج — اختبارات المحرك الكاملة (قائمة §8-3) تمر + فحصك النهائي.
- [ ] كل مرجع في المخرجات مرسوم من الفهرس (فحص آلي).
- [ ] نصف الوجه من الأسطر الفعلية للصفحات (لا رقم ثابت).
- [ ] الكبرى «كل يوم دراسي» بمقدار تحدده المعلمة، بلا سقف جزاء (قرار المستخدمة).
- [ ] العينة PDF العربية تُفتح بفحص بصري سليم.
- [ ] `npm run verify` كامل **يبقى أخضر طوال المرحلة** (لا كسر).
- [ ] بيئة التجربة و`scratch/` محذوفان/مستثنيان قبل الإنتاج.

---

## 12) مطلوب من صاحبة المشروع قبل التنفيذ

1. الموافقة الصريحة على هذه الوثيقة (النسخة 2).
2. **تنزيل مادة حدود الأجزاء من QUL** (خطوة يدوية واحدة — التحميل يتطلب حسابها): الدخول إلى QUL ← `https://qul.tarteel.ai/resources/quran-metadata/68` ← «Download json» ← الحفظ في `data/mushaf/quran-metadata-juz.json`.
3. السماح بخطوة الشبكة الواحدة لتحميل خط IBM Plex Sans Arabic.
4. الالتزام بالمشاركة في **الفحص اليدوي** بعد كل طبقة (بوابات G1–G4) في بيئة المتصفح المؤقتة.
