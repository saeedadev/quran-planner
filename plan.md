# خطة مشروع تواسع — تخطيط حلقات تحفيظ القرآن الكريم

> هذه الوثيقة هي **النسخة المعتمدة النهائية** للخطة (بعد مراجعة خمسة مراجعين ودمج ملاحظات صاحبة المشروع).
> **لا تُعدَّل إلا بموافقة صريحة** من صاحبة المشروع.

### سجل التعديلات المعتمدة

| # | التاريخ | التعديل | البند المتأثر |
|---|---|---|---|
| 1 | 13/8/2026 | اعتماد **QUL** (`qul.tarteel.ai`) كمصدر معتمد رسمياً لبيانات مصحف المدينة — تخطيط **KFGQPC V4 (1441H print) — Layout 19** | §2، §3 |
| 2 | 13/8/2026 | أسماء السور تُشتق من داخل مواد QUL إن توفرت؛ وإلا يُعتمد `quran-metadata` كمصدر معاون موثق عند الحاجة | §3 |
| 3 | 13/8/2026 | صيغة تنزيل مواد QUL: **JSON** (مُوصى بها)، و**SQLite مثبت الإصدار** بديل عند الحاجة | §3 |
| 4 | 13/8/2026 | الفهرس المدمج **لا يتضمن نصوص الآيات** — بيانات مرجعية/تخطيطية فقط | §3، §4-6 |

---

## 1) نبذة عن المشروع

تطبيق ويب يعمل على الجوال والتابلت والديسكتوب (مع إمكانية التحويل لتطبيق جوال لاحقاً)، يساعد معلمات حلقات تحفيظ القرآن على بناء خطط حفظ ومراجعة، من إدخال بسيط لمعلمات الخطة حتى إخراج ملف PDF يُنزَّل على الجهاز وجاهز للطباعة.

**من يستخدمه:** أكاديمية واحدة لتحفيظ القرآن، تتكون من إدارة وإشراف ومعلمات وحلقات.

**أبرز المتطلبات:**
- لكل حلقة معلمة واحدة، ويمكن للمعلمة إدارة أكثر من حلقة.
- الخطط المحفوظة مشتركة: أي معلمة تستطيع الإطلاع على أي خطة، وأخذ نسخة منها وتعديلها وطباعتها **دون المساس بالأصل**.
- المعلمة تعدّل يدوياً على المعاينة قبل إنشاء الخطة، وتستطيع لاحقاً فتح الخطة المحفوظة ومراجعتها.
- شعار الأكاديمية واسمها يُداران من صفحة إعدادات، ويظهران في التطبيق وعلى الـ PDF دون تعديل الكود.
- الحسابات والصلاحيات الكاملة تُضاف في نسخة لاحقة (النسخة الأولى تعمل بدون حسابات وبدون خادم).

---

## 2) القرارات المعتمدة

| القرار | النتيجة |
|---|---|
| الدخول | النسخة الأولى **بدون حسابات**، والحسابات والصلاحيات في نسخة ثانية |
| التشغيل | النسخة الأولى تعمل **في المتصفح مباشرة على أي جهاز**، دون خادم ودون إنترنت |
| المشاركة والنسخ الاحتياطي | عبر **تصدير/استيراد ملفات JSON** (إرسال خطة لمعلمة أخرى، أو نسخة احتياطية كاملة) |
| تعديل الخطط المحفوظة | يُنشأ دائماً **نسخة جديدة** تُعدَّل وتُطبع، والأصلية لا تتغير أبداً |
| مراجعة الخطة بعد إنشائها | الخطة المنشأة قابلة للفتح والمراجعة والتعديل مع **تراجع (Undo)** داخل المحرر |
| الحذف | في النسخة الأولى **أرشفة فقط**؛ الحذف النهائي في النسخة الثانية مع الحسابات |
| اعتماد الخطط | الخطة تُعتبر نهائية فور إنشائها (لا مرحلة موافقة) |
| خطط الطالبات | في النسخة الأولى: **خطة عامة للحلقة** — تؤجَّل الخطط الفردية للطالبات إلى نسخة لاحقة |
| مصدر المصحف | **QUL** — تخطيط KFGQPC V4 (1441H print) — Layout 19 — بدون نصوص آيات في الفهرس *(تعديل معتمد 13/8/2026)* |
| السرد التثبيت | هو يوم المراجعة الأسبوعية: لا حفظ جديد، مراجعة شاملة من بداية السورة الحالية إلى نقطة الحفظ |
| السرد مع الرفيق | لا حفظ جديد، مراجعة المحفوظ مع زميلة |
| نمط الأسبوع | لكل يوم من الأيام السبعة نوع: دراسة / سرد تثبيت / سرد مع رفيق / إجازة |
| أيام الإجازة | تظهر فيها **عبارة دينية/تشجيعية** بدل كلمة "إجازة" أو خلية فارغة (والجمعة والسبت إجازة غالباً، وقد يجعلها بعض المعلمات أيام دراسة) |
| المراجعة الكبرى | تدخل في **كل يوم دراسي**، بمقدار ودورية تختارهما المعلمة لكل خطة، وسقف أقصى **جزء واحد يومياً** |
| تواريخ الجدول | **الميلادي** في النسخة الأولى، ويُضاف **الهجري** في النسخة الثانية |
| ملف PDF | يُنشأ و**يُنزَّل كملف** على الجهاز (لا يُعرض على الشاشة فقط) |
| مدخلات المستخدم | تُتحقق دائماً في طبقة واحدة مشتركة (وعند السحابة تُعاد في الخادم) |

---

## 3) مصدر بيانات مصحف المدينة (المعتمد رسمياً)

- **المصدر المعتمد رسمياً:** **QUL** — `https://qul.tarteel.ai` — تخطيط المصحف **KFGQPC V4 (1441H print)** — **Layout 19** (نسخة مجمع الملك فهد 1441هـ — خط عثمان طه — رواية حفص عن عاصم). *(تعديل معتمد 13/8/2026)*
- **المواد المطلوبة من QUL:**
  1. تخطيط الصفحات (`pages`): صفحة ← سطر ← نوع السطر ← نطاق كلمات، لجميع الصفحات (1–604).
  2. بيانات الكلمات المطابقة لتخطيط V4 — تُستخدم **بنيوياً فقط** لاشتقاق نطاق آيات كل سطر (لا تُخزَّن نصوصها في الفهرس).
  3. أسماء السور: تُشتق من داخل مواد QUL إن توفرت؛ وإلا يُعتمد `quran-metadata` كمصدر معاون موثق عند الحاجة *(تعديل معتمد 13/8/2026)*.
- **صيغة التنزيل:** **JSON** (مُوصى بها — بلا اعتماد على مكتبات SQLite أصلية على ويندوز)؛ و**SQLite مثبت الإصدار** بديل عند الحاجة *(تعديل معتمد 13/8/2026)*.
- تُحفظ المواد المُنزلة في مجلد `data/mushaf/` داخل المشروع، ويُوثَّق أصلها وسلامتها في `data/qul-source-manifest.json` (نسب بشري + هاش SHA-256) — خارج مجلد الفهرس المُبنى.
- التطبيق **يقرأ من الفهرس المدمج فقط ولا يغيّر البيانات الأصلية أبداً**.
- كل مرجع حفظ أو مراجعة يكون دائماً "سورة:آية" يُرسم من الفهرس، لا نصاً مكتوباً يدوياً.
- يُبنى من مواد QUL مرة واحدة **فهرس مدمج** يُضمَّن داخل التطبيق (ليعمل دون إنترنت من ملف محلي مباشرة)، مع **توقيع (هاش)** للتحقق، والفهرس مستمد رسمياً من المصدر المعتمد ولا يُستبدل ببيانات أخرى.
- **نطاق الفهرس *(تعديل معتمد 13/8/2026)*:** بيانات مرجعية/تخطيطية فقط — بنية الصفحة والسطر، وأرقام السور والآيات والصفحات — **بدون نصوص الآيات**.

---

## 4) مخطط قاعدة البيانات (في المتصفح محلياً)

1. **إعدادات الأكاديمية** — سجل واحد: اسم الأكاديمية + الشعار (صورة من نوع PNG/JPG/WebP تُفحص). يُعدَّل من شاشة الإعدادات دون لمس الكود.
2. **المعلمات** — سجل بأسماء المعلمات (الاسم فقط في النسخة الأولى، ويُربط بالحسابات لاحقاً).
3. **الحلقات** — سجل: اسم الحلقة + المعلمة المسؤولة (معلمة واحدة قد تدير أكثر من حلقة).
4. **الخطط** — اسم الخطة + الحلقة + النوع (محدد / مفتوح) + الاتجاه (أمامي / معكوس) + نقطة البداية (سورة وآية) + تاريخا البداية والنهاية (للمحدد) أو عدد الأسابيع (للمفتوح) + المعدل اليومي (نصف وجه / وجه كامل / عدد آيات) + نمط الأسبوع + إعدادات المراجعة الكبرى (الدورية والمقدار والسقف) + حقل "مشتقة من خطة" (النسخ) + تاريخ الإنشاء/التعديل + حالة أرشيف.
5. **أيام الخطة (جدول التفاصيل)** — صف لكل يوم: التاريخ (الميلادي في v1) + اليوم + نوع اليوم + الحفظ الجديد (من آية إلى آية) + المراجعة الصغرى + المراجعة الكبرى + **العبارة** (لأيام الإجازة) + التعديل اليدوي (يُحفظ منفصلاً عن القيمة المحسوبة).
6. **بيانات المصحف المرجعية** — فهرس مدمج للقراءة فقط (ليست جدولاً نكتبه): رقم السورة والآية والصفحة، وتفصيل أسطر كل صفحة لحساب نصف الوجه بدقة — **بيانات مرجعية/تخطيطية فقط، دون نصوص الآيات** *(تعديل معتمد 13/8/2026)*.
7. **الحسابات (نسخة ثانية لاحقاً)** — لكل مستخدم حساب ودور (إدارة / إشراف / معلمة)، ليُفعَّل منع الحذف ويُسجَّل من أنشأ كل خطة، بالإضافة إلى **مكتبة العبارات** (إضافة يدوية، استيراد من Excel، تصفح، اختيار عبارتين فأكثر لتتكرارا في الخطة، أو اختيار عشوائي).

**العلاقات:** معلمة ← حلقات عديدة | حلقة ← خطط عديدة | خطة ← أيام عديدة | خطة قد تكون نسخة من خطة أخرى.

---

## 5) التقنيات

| الجزء | الاختيار | السبب |
|---|---|---|
| الواجهة | React + Vite + TypeScript | تعمل على كل الأجهزة، وقابلة للتحويل لتطبيق جوال لاحقاً، وتُبنى كتطبيق ثابت يُفتح في المتصفح مباشرة |
| التخزين | **طبقة وصول بيانات موحدة**: IndexedDB (مع بديل localStorage) | تعمل محلياً على أي جهاز، وهذه الطبقة **وحدها** تُستبدل بخدمة سحابية لاحقاً دون لمس بقية الكود |
| ملف PDF | pdfmake + خط عربي مضمّن (مثل IBM Plex Sans Arabic — مرخّص للتضمين) | يدعم العربية والاتجاه من اليمين لليسار، ويُنشئ ملفاً يُنزَّل |
| الاختبار | Vitest | نفس نظام Vite، مناسبة للمشاريع الصغيرة |

---

## 6) بنية الطبقات

1. **طبقة الشاشات (الواجهة):** ما تراه المعلمة وتضغطه.
2. **طبقة المحرك الحسابي:** قلب التطبيق — يحسب الجدول، ولا يعرف الشاشة ولا التخزين (منطق خالص، قابل للنقل والاختبار).
3. **طبقة الوصول للبيانات:** واجهة موحدة (حفظ/استرجاع/نسخ/أرشفة/تصدير/استيراد) بتنفيذ محلي — **وحدها** تُستبدل عند الانتقال للسحابة.
4. **طبقة بيانات المصحف:** ملفات مرجعية تُقرأ فقط + فهرس مدمج.
5. **طبقة الـ PDF:** توليد الملف وتنزيله.

---

## 7) شجرة المشروع (ملفات نظيفة — لا شيء زائد)

```
tawasu/
├─ data/mushaf/            ← مواد QUL المُنزلة (مرجعية، قراءة فقط)
├─ data/qul-source-manifest.json ← سجل نسب مواد QUL وهاشها (SHA-256)
├─ tools/build-mushaf.ts   ← يبني الفهرس المدمج من مواد QUL + التحقق (114/6236/604) + هاش
├─ shared/                 ← أنواع مشتركة + قواعد تحقق (تُستخدم من كل الطبقات)
├─ src/
│  ├─ pages/               ← الشاشات
│  ├─ components/          ← المكونات المتكررة (منها محرر المعاينة)
│  ├─ engine/              ← المحرك الحسابي (حفظ/مراجعة صغرى وكبرى/مصحف)
│  ├─ storage/             ← واجهة البيانات + تنفيذ IndexedDB (يُستبدل سحابياً لاحقاً)
│  ├─ pdf/                 ← توليد PDF وتنزيله
│  ├─ data/                ← الفهرس المدمج داخل الحزمة (ليعمل دون إنترنت)
│  └─ styles/
├─ tests/                  ← اختبارات المحرك والفهرس (Vitest)
├─ package.json
└─ README.md
```

---

## 8) المحرك الحسابي — خطوة بخطوة

1. المعلمة تدخل: نقطة البداية، المعدل اليومي، نمط الأسبوع، المدة (تواريخ أو أسابيع)، الاتجاه، إعدادات المراجعة الكبرى.
2. المحرك يبني قائمة الأيام طوال المدة ويحدد نوع كل يوم من نمط الأسبوع.
3. كل يوم دراسة: يُضاف **الحفظ الجديد** بمقدار المعدل (نصف وجه / وجه كامل / عدد آيات)، ويتحرك المؤشر آيةً آية حسب الاتجاه (أمامي: الفاتحة ← الناس، معكوس: الناس ← الفاتحة).
4. **المراجعة الصغرى:** من بداية السورة الحالية إلى مؤشر الحفظ الحالي، تُقسَّم بالتساوي على أيام المراجعة في الأسبوع، **ما عدا أول يوم بعد السرد الأسبوعي** (لا صغرى فيه إطلاقاً).
5. **السرد التثبيت (المراجعة الأسبوعية):** لا حفظ جديد، مراجعة شاملة من بداية السورة الحالية إلى نقطة الحفظ.
6. **السرد مع الرفيق:** لا حفظ جديد، مراجعة المحفوظ مع زميلة.
7. **المراجعة الكبرى:** كل يوم دراسي يأخذ نصيباً من المحفوظ المكتمل (كل السور المكتملة قبل السورة الجارية)، ينمو حسب الدورية والمقدار اللذين تختارهما المعلمة، بسقف أقصى **جزء واحد يومياً لا يُتجاوز أبداً**، وعند الوصول لنهاية المحفوظ المكتمل تعود الدورة من البداية.
8. عند اكتمال سورة كاملة: تخرج من المراجعة الصغرى وتدخل ضمن المراجعة الكبرى، وتبدأ الصغرى من السورة الجديدة.
9. **المعاينة:** جدول كامل قابل للتعديل يدوياً (اسم الحلقة، اسم المعلمة، التواريخ عند الخطأ، والعبارات) — التعديل اليدوي لأي يوم يُحفظ منفصلاً ولا يُلغيه إعادة الحساب، مع إمكانية **التراجع (Undo)** داخل المحرر.
10. الحفظ، ثم توليد الـ PDF وتنزيله.

**ملاحظات على الحدود:**
- **وضع "المفتوح"** يحسب نقطة النهاية ويعرضها صراحةً في المعاينة والـ PDF.
- **نصف الوجه** يُحسب من **الأسطر الفعلية** لكل صفحة من الملفات (بالتناوب، يغطي كل وجهين متتاليين الصفحة كاملة) — لا افتراض ثابت لعدد الأسطر.
- عند وصول الحفظ المعكوس إلى نهاية الفاتحة تتوقف الخطة عند آخر آية دون آيات وهمية.
- رفض نمط أسبوع بلا أيام دراسة، وتواريخ معكوسة، ومعدل صفري، وسقف أقصى لمدة الخطة — كلها برسائل واضحة.
- عند إعادة الحساب تُفحص القيم اليدوية المخزنة؛ إن أصبحت خارج النطاق الجديد تُخطر المعلمة بها بدل تجاهلها.

---

## 9) شاشات التطبيق

1. **الرئيسية:** الخطط المحفوظة + زر خطة جديدة (مع بحث وفلترة حسب الحلقة).
2. **إنشاء خطة** (مراحل متتابعة): الأساسيات ← المدة ← نمط الأسبوع ← المعدل والمراجعة ← المعاينة القابلة للتعديل والتراجع.
3. **الخطط المحفوظة:** فتح، مراجعة وتعديل (ينشئ نسخة جديدة لا يمس الأصل)، تراجع، أرشفة، طباعة/تنزيل PDF، **تصدير/استيراد**.
4. **المعلمات والحلقات:** إضافة معلمة وحلقة ببساطة.
5. **الإعدادات:** اسم الأكاديمية + رفع الشعار + **تصدير/استيراد نسخة احتياطية كاملة** — يظهران في التطبيق وعلى الـ PDF مباشرة.

---

## 10) ملف PDF

- **صفحة ملخص:** الشعار + اسم الأكاديمية + الحلقة والمعلمة + النوع والاتجاه + المدة + نقطة البداية والنهاية المحسوبة + المعدل.
- **جدول يومي:** لكل يوم (اليوم، التاريخ **الميلادي**، نوع اليوم، الحفظ الجديد، المراجعة الصغرى، المراجعة الكبرى، **والعبارة** لأيام الإجازة).
- **رأس كل صفحة:** شعار واسم الأكاديمية + اسم الخطة.
- **يُولَّد ويُنزَّل كملف PDF على الجهاز** (لا يُعرض على الشاشة فقط).
- ملاحظة: لا يُدرج نص الآيات ولا سطر توقيع في هذه النسخة (حسب اختيار صاحبة المشروع)، والتاريخ الهجري يُضاف في النسخة الثانية.

---

## 11) استراتيجية الاختبار

- **الأداة:** Vitest، مع أوامر `npm test` و `npm run test:mushaf`.
- **اختبار المحرك:** الحفظ المحدد والمفتوح، الأمامي والمعكوس، المعدلات الثلاثة، نمط أسبوع بلا أيام سرد، الصغرى (التوزيع، أول يوم بعد السرد بلا صغرى، حدود السورة)، الكبرى (الدورية، المقدار، سقف جزء/يوم، العودة من البداية)، انتقال السورة من الصغرى للكبرى، نهاية الخطة في منتصف سورة، **عدم مساس إعادة الحساب بالتعديل اليدوي**، و**ظهور العبارات في أيام الإجازة**.
- **اختبار بيانات المصحف:** الأرقام الرسمية (114 سورة، 6236 آية، 604 صفحات) محسوبة من الملفات، تكرارية الفهرس، نصف الوجه بالأسطر الفعلية، وترقيم مستمر بين الصفحات.
- **اختبار الواجهة:** قائمة تحقق يدوية على المقاسات 375/768/1280 + الحالات الفارغة والخطأ + اختبار التصدير/الاستيراد (دائرة مغلقة) + التراجع + تنزيل الـ PDF.
- **فحص الـ PDF:** بصري يدوي (الملخص، الجدول، الرأس، الشعار، ظهور العبارات، الاتجاه العربي السليم).
- **بوابة تسليم:** لا يُسلَّم أي إصدار قبل نجاح `npm test`.

---

## 12) مراحل التنفيذ

1. إعداد المشروع + إحضار ملفات مصحف المدينة وبناء الفهرس + التحقق من الأرقام + اختبارات الفهرس.
2. المحرك الحسابي واختباراته + تجربة PDF عربية مبكرة (كشف المخاطرة أولاً).
3. طبقة البيانات المحلية (IndexedDB) + حفظ/نسخ/أرشفة/تصدير/استيراد.
4. شاشات الإنشاء والمعاينة (محرر التعديل والتراجع).
5. توليد الـ PDF كاملاً وتنزيله.
6. الإعدادات والشاشات المتبقية.
7. الفحص الشامل على المقاسات الثلاثة + PDF + الحالات الفارغة والخطأ.

---

## 13) حدود النسخة الأولى (منصوص عليها)

- بدون حسابات وبدون خادم وبدون سحابة: يعمل في المتصفح على جهاز أي معلمة، والمشاركة عبر تصدير/استيراد الملفات.
- **أرشفة** بدل الحذف النهائي (الحذف النهائي والصلاحيات ومكتبة العبارات الكاملة والتاريخ الهجري في النسخة الثانية).
- الخطة للحلقة العامة فقط (الخطط الفردية للطالبات تؤجل).
- لا تُعدَّل هذه الوثيقة إلا بموافقة صريحة من صاحبة المشروع.
