# خطة تنفيذ المرحلة الأولى — مشروع تواسع

> وثيقة تشغيلية مستمدة من `plan.md` (النسخة المعتمدة) وبعد التعديلات المعتمدة بتاريخ **13/8/2026**.
> **لا يُنفَّذ أي كود قبل موافقة صاحبة المشروع الصريحة على هذه الوثيقة.**

---

## 1) الهدف والنطاق

- **الهدف:** تجهيز المشروع، إحضار مواد QUL المعتمدة، بناء الفهرس المدمج، التحقق من الأرقام الرسمية، واختبارات الفهرس — حسب بند (1) من مراحل التنفيذ في `plan.md` §12.
- **النطاق يشمل:** هيكل المشروع، الأوامر الأساسية، التنزيل والتوثيق، `shared/`، أداة البناء، الفهرس المدمج، اختبارات المصحف، بوابة التسليم.
- **خارج النطاق (مراحل لاحقة):** المحرك الحسابي، التخزين المحلي، الشاشات، الـ PDF — لا يُلمس أي منها في هذه المرحلة.

---

## 2) القرارات المعتمدة الملزمة لهذه المرحلة

| القرار | النتيجة |
|---|---|
| المصدر | **QUL** — `https://qul.tarteel.ai` — تخطيط **KFGQPC V4 (1441H print) — Layout 19** |
| مواد المصدر | (1) تخطيط الصفحات والأسطر، (2) بيانات كلمات V4 (بنيوياً فقط)، (3) أسماء السور من QUL أو `quran-metadata` عند الحاجة |
| صيغة التنزيل | **JSON** (مُوصى بها)؛ **SQLite مثبت الإصدار** بديل عند الحاجة |
| الفهرس | **بيانات مرجعية/تخطيطية فقط — بدون نصوص الآيات** |
| الأرقام المرجعية | 114 سورة / 6236 آية / 604 صفحات — تُحسب من الملفات لا من منشور ثانٍ |

---

## 3) المصدر والمواد — بالتفصيل

1. **المصدر الوحيد المعتمد:** QUL (تخطيط KFGQPC V4 — 1441H print — Layout 19). لا يُخلط مع أي مصدر آخر.
2. **المادة 1 — تخطيط الصفحات:** لكل صفحة (1–604): قائمة الأسطر، ونوع كل سطر، ونطاق كلماته. هذا الأساس لحساب نصف الوجه بالأسطر الفعلية (§8 في plan.md).
3. **المادة 2 — بيانات الكلمات:** بيانات الكلمات المطابقة لتخطيط V4، تُستخدم **بنيوياً فقط** (حرفياً: لاشتقاق نطاق آيات كل سطر). **لا تُخزَّن نصوصها في الفهرس إطلاقاً.**
4. **المادة 3 — أسماء السور:** تُشتق من داخل مواد QUL إن وُجدت؛ فإن تعذرت، يُعتمد `quran-metadata` **كمصدر معاون موثق** ويُسجَّل ذلك صراحةً في manifest قبل الاستخدام.
5. **التوثيق:** كل مادة مُنزلة تُوثَّق في `data/qul-source-manifest.json`: المصدر، رابط الصفحة، الإصدار، تاريخ التنزيل، طريقة المعاينة البشرية، و**هاش SHA-256** لكل ملف.
6. **حفظ المواد:** تُحفظ في `data/mushaf/` (مرجعية، قراءة فقط). لا تُضمَّن داخل حزمة التطبيق — الفهرس المدمج فقط هو ما يُضمَّن في `src/data/`.

---

## 4) السلوك الإلزامي (من مراجعات الخمسة وقواعد المشروع)

- **فشل صريح (fail-closed):** أي تناقض أو ملف ناقص أو رقم غير منطقي **يوقف البناء برسالة واضحة** — لا تصحيح، لا استنتاج، لا افتراض لبيانات ناقصة.
- **قابلية التكرار (deterministic):** نفس المواد ⇒ نفس الفهرس.
  - هاش SHA-256 على أثر البناء النهائي.
  - ترتيب مفاتيح ثابت ومجرَّد (canonical) في الفهرس.
  - UTF-8 بلا BOM.
  - **لا بيانات زمنية (تواريخ ساعة التنفيذ) داخل أثر الفهرس** — حتى لا يختلف أثران.
- **مصدر واحد:** لا خلط مع أي مصدر آخر سوى المصادر المعتمدة في §2 و§3.
- **التحقق قبل التسليم:** أرقام 114/6236/604 وترقيم مستمر بين الصفحات تُتحقق آلياً.

---

## 5) خطوات التنفيذ بالترتيب

1. **هيكل المشروع**
   - تهيئة Vite + React + TypeScript، وإضافة Vitest.
   - إنشاء المجلدات حسب شجرة `plan.md` §7: `data/mushaf/`، `tools/`، `shared/`، `src/pages|components|engine|storage|pdf|data|styles`، `tests/`.
   - أوامر `package.json`: `dev`، `build`، `test`، `test:mushaf`، `build:mushaf`، و**`verify`** (البوابة: `build:mushaf` ثم `test:mushaf` ثم `npm test`).
   - `README.md` يتضمن: المصدر المعتمد، كيفية إعادة البناء، وكيفية فحص الملفات يدوياً.

2. **إحضار مواد QUL وتوثيقها**
   - تنزيل مادة التخطيط (كل الصفحات 1–604) بصيغة JSON إلى `data/mushaf/`.
   - تنزيل مادة الكلمات البنيوية اللازمة لاشتقاق نطاقات الآيات.
   - استخراج أسماء السور من داخل مواد QUL (أو تفعيل خيار `quran-metadata` الموثق عند تعذرها).
   - كتابة `data/qul-source-manifest.json` بكل البيانات الموثقة (بند 3-5).
   - **خطوة فحص بشري:** مراجعة عينة من الصفحات (الفاتحة، بداية البقرة، منتصف المصحف، نهاية الناس) للتأكد من سلامة المواد قبل البناء.

3. **`shared/` — الأنواع وقواعد التحقق**
   - أنواع: سورة، آية، صفحة، سطر، مرجع "سورة:آية"، نطاق كلمات، أثر الفهرس.
   - قواعد تحقق موحدة تُستخدم من أداة البناء ومن الاختبارات معاً (طبقة تحقق واحدة، كما في `plan.md` §2).

4. **أداة البناء `tools/build-mushaf.ts`**
   - قراءة مواد QUL من `data/mushaf/`.
   - التحقق: 114 سورة، 6236 آية، 604 صفحات، **استمرارية الترقيم بين الصفحات**، اكتمال نطاقات الأسطر.
   - اشتقاق نطاق آيات كل سطر من مادة الكلمات **بنياً** (دون تخزين نصوص).
   - بناء الفهرس المدمج بترتيب مفاتيح ثابت، **دون نصوص آيات**.
   - إنتاج الهاش (SHA-256) وكتابة الفهرس إلى `src/data/` بصيغة JSON (UTF-8 بلا BOM).
   - **فحص البناء مرتين:** بناء متكرر يجب أن يعطي الهاش نفسه (اختبار التكرارية).

5. **اختبارات المصحف `tests/mushaf/` (Vitest)**
   - الأرقام الرسمية (114/6236/604) محسوبة من الملفات الفعلية.
   - استمرارية الترقيم بين الصفحات.
   - نصف الوجه بالأسطر الفعلية (بالتناوب، يغطي كل وجهين الصفحة كاملة).
   - تكرارية البناء (الهاش ثابت).
   - صلاحية كل "سورة:آية" في الفهرس (المرجع يُرسم من الفهرس لا نصاً يدوياً).
   - `npm run test:mushaf` يقرأ `data/mushaf/` **مباشرة** (بيانات حقيقية).

6. **اختبارات دائمة `npm test`**
   - اختبارات على الفهرس المدمج داخل الحزمة (تعمل دون مواد ودون شبكة) — تحمي الفهرس من الكسر عند أي تعديل لاحق.

7. **بوابة التسليم `npm run verify`**
   - تعمل كاملة (بناء + فحص مزدوج + اختبارات) قبل أي اعتبار للمرحلة مكتملة.

---

## 6) استراتيجية الاختبار والبوابة

| الأمر | الغرض | متى يُشغَّل |
|---|---|---|
| `npm run build:mushaf` | بناء الفهرس من مواد QUL + التحقق + الهاش | عند تغيير المواد أو الأداة |
| `npm run test:mushaf` | اختبارات على المواد الفعلية في `data/mushaf/` | نفس الوقت |
| `npm test` | اختبارات الفهرس المدمج داخل الحزمة (دائمة) | كل تشغيل عادي |
| `npm run verify` | **البوابة النهائية** = `build:mushaf` ← `test:mushaf` ← `npm test` | قبل كل تسليم |

- لا يُسلَّم أي إصدار قبل نجاح `npm run verify`.

---

## 7) معايير القبول (قابلة للقياس)

- [ ] 604 صفحة مكتملة النطاقات، 114 سورة، 6236 آية — تلقائياً ودون أخطاء.
- [ ] ترقيم "سورة:آية" متصل عبر حدود الصفحات بلا ثغرات ولا تكرار.
- [ ] نصف الوجه محسوب بالأسطر الفعلية لكل صفحة.
- [ ] البناء متكرر: تشغيلان متتاليان يعطيان هاشاً واحداً.
- [ ] الفهرس خالٍ من نصوص الآيات (فحص آلي في الاختبار).
- [ ] `data/qul-source-manifest.json` موثَّق وكامل، وفحص بشري لعينة الصفحات تم وتُسجَّل نتيجته.
- [ ] `npm run verify` ينجح كاملاً.

---

## 8) الحالات الفارغة والخطأ التي يجب معالجتها

- صفحة بدون أسطر، أو سطر بدون نطاق كلمات، أو ملف ناقص.
- نطاق كلمات لا يُطابق بداية/نهاية صفحة مجاورة (انقطاع الترقيم).
- رقم سورة/آية غير متوقع عند حدود الصفحات (خاصة أوائل السور).
- صفحات خاصة: صفحة 1 (الفاتحة + البسملة سطراً مستقل)، أوائل سورة البقرة، صفحة 604 (نهاية الناس).
- تعذُّر الحصول على أسماء السور من مواد QUL (تفعيل `quran-metadata` الموثق فقط، مع تسجيل القرار في manifest).
- كل حالة خطأ: **إيقاف صريح برسالة واضحة**، لا إكمال جزئي ولا تصحيح صامت.

---

## 9) المخاطر والقرارات المفتوحة

| المخاطرة/القرار | الحالة | الخطة |
|---|---|---|
| تنسيق بنية QUL الفعلية للمواد | تفتح عند التنزيل | تُستخرج البنية من المواد نفسها، وتُوثَّق في manifest، دون افتراضات غير مؤكدة |
| صيغة JSON مقابل SQLite | **مقرر:** JSON (تُجنَّب مكتبات أصلية على ويندوز) | إن تبيَّن أن مواد QUL الضخمة لا تتناسب مع JSON عملياً، يُعرض على صاحبة المشروع خيار SQLite قبل التغيير |
| أسماء السور | شرطي (QUL أو `quran-metadata`) | لا يُستخدم المصدر المعاون إلا عند تعذُّر QUL، وبتسجيل صريح |

---

## 10) ما أُنجِز الآن (توثيقي فقط — لا كود)

- **عُدِّل `plan.md`** (بموافقة صاحبة المشروع):
  - إضافة **سجل التعديلات المعتمدة** أعلى الوثيقة.
  - §2 (قرار "مصدر المصحف"): QUL — KFGQPC V4 — Layout 19 — بلا نصوص آيات.
  - §3 (مصدر البيانات): QUL مصدراً رسمياً، تفصيل المواد الثلاث، JSON/SQLite، `data/qul-source-manifest.json`.
  - §4 بند 6: الفهرس بلا نصوص آيات.
  - §7 (الشجرة): إضافة manifest وإعادة تسمية مجلد المواد.
- **أُنشئت هذه الوثيقة** (`plan-phase1.md`) كخطة تنفيذ المرحلة الأولى.

---

## 11) المطلوب من صاحبة المشروع قبل التنفيذ

1. الموافقة على هذه الوثيقة (`plan-phase1.md`) صراحةً.
2. تأكيد السماح بالوصول إلى QUL لتنزيل المواد (خطوة إحضار المواد تتطلب شبكة مرة واحدة).
3. أي ملاحظة على البنود المفتوحة في §9.
